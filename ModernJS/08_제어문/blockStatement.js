// 문의 마침표인 세미콜론(;)을 사용하는 것이 맞으나, 블록{}은 자체 종결성을 갖기 때문에 끝에 세미콜론을 붙이지 않는다.
// 블록문
{
    var foo = 10;
}

// 제어문
var i = 1;
if ( i < 10 ) {
    i++;
}

// 함수 선언문
function sum(a, b) {
    return a + b;
}
